<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('partials/head', { title: 'Log In' }) %>
</head>

<body>
    <header class="container">
        <div id="header-container">
          <div>
            <a href="/"><img src="/images/tribook.svg" alt="tribook-logo" id="logo"></a>
          </div>
          <nav>
            <div>
              <% if (isAuthenticated) { %>
                  <a id="login-button" href="/dashboard"><button>Dashboard</button></a>
                  <a id="login-button" href="/logout"><button>Log Out</button></a>
                <% } else { %>
                  <a id="login-button" href="/sign-up"><button>Sign Up</button></a>
                    <% } %>
            </div>
          </nav>
        </div>
      </header>

        <main class="container">

            <form method="POST" action="/login">
                <input type="text" name="username" placeholder="Username" required />
                <input type="password" name="password" placeholder="Password" required />
                <button type="submit">Log In</button>
                <% if (loginMessage) { %>
                    <div id="message">
                        That user already exists, please proceed to log in with your username and password
                    </div>
                    <% } else { %>
                        <p></p>
                        <% } %>
            </form>
            
        </main>
</body>

</html>